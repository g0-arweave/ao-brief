---
title: "AO Daily: Sam on HyperBEAM - 2026-09-19"
brief_type: "daily"
date_pt: "2026-09-19"
generated_at_utc: "2026-09-19T17:18:15Z"
---

# AO Daily: Sam on HyperBEAM - 2026-09-19

AO Daily Build Brief  
Date: 2026-09-19

Short version:  
AO work today tightened HyperBEAM’s default setup so node behavior is more explicit and less likely to depend on a gateway store unless the operator asks for it.

## Sam on HyperBEAM

### Quick read:
Today’s HyperBEAM change is small, but useful: it removes the gateway store from the default options. For builders and operators, the practical effect is cleaner startup/config behavior — fewer hidden assumptions about where a node reads or stores gateway-related data.

That fits the recent thread well. After the larger realtime GraphQL query work and follow-up fixes around indexing, pagination, filters, and release builds, this is a cleanup pass on defaults. It is the kind of change that helps keep new query and storage paths from adding surprise behavior for people running nodes.

Technically, the change landed through PR #1137 and only touched `src/core/resolver/hb_opts.erl`, deleting 14 lines from the default options path.

### Why it matters:
- Easier operation: node defaults stay simpler, and operators can choose gateway-store behavior instead of inheriting it by default.
- Fewer edge cases: as HyperBEAM’s query stack gets more capable, keeping defaults explicit helps avoid confusing differences between local setups.

### What stands out:
Good progress on reducing setup friction after a larger query-stack push. The direction here is practical hardening: make the powerful parts available, but keep the default path clean.

### Receipts:
- [`Merge pull request #1137 from permaweb/fix/remove-default-gateway-store`](https://github.com/permaweb/HyperBEAM/commit/3db10473addf59d0b81f5695e0591672c73918d7)  
  PR: [`fix: remove gateway store from default opts`](https://github.com/permaweb/HyperBEAM/pull/1137)  
  Changed `src/core/resolver/hb_opts.erl`, deleting 14 lines. Practical read: gateway-store behavior is no longer part of the default options, which makes node configuration more explicit.

### Suggested X post:
Small but useful HyperBEAM cleanup today: gateway store was removed from default opts.

After the recent realtime GraphQL/indexing work, this kind of default cleanup matters. It keeps node setup clearer and reduces surprise behavior for operators.

### Thread:
1/ AO progress today was a focused HyperBEAM cleanup: remove gateway store from default options.

Small diff, clear practical effect — node behavior becomes more explicit instead of assuming gateway-store use by default.

2/ This follows the recent realtime GraphQL + indexing work, where HyperBEAM gained fresher query paths and more moving parts around storage.

After that kind of change, clean defaults matter.

3/ The merged PR was `fix: remove gateway store from default opts`, touching `src/core/resolver/hb_opts.erl` with a 14-line deletion.

Good sign of hardening: fewer hidden assumptions, easier operation, clearer setup.

Source links:
- Sam on HyperBEAM: https://github.com/permaweb/HyperBEAM/commits?author=samcamwilliams
